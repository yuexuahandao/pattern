If user want to use his data to system's data,
we can use an adapter to meet with it.

System cannot know what data user will define,
but system can confirm what format data it can display.
Like java api's JTable.It use TableModel to transfer the data's format.